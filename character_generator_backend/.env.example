# Backend environment variables

# PUBLIC_INTERFACE
# ADMIN_TOKEN is used for admin endpoints simple authentication
ADMIN_TOKEN=dev-admin-token

# PUBLIC_INTERFACE
# SESSION_SECRET is used for session middleware cookie signing
SESSION_SECRET=dev-secret
